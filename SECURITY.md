# Security Policy

Do not publish API keys, local model paths containing private usernames, repository secrets, or `.zenie/` session data in issues.

For sensitive vulnerabilities, use GitHub private vulnerability reporting when enabled on the repository. Otherwise contact the maintainer privately before public disclosure.

Zenie Code can execute tests, shell commands, and patches. Review permission settings before using it on untrusted repositories.
